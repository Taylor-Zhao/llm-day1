# Day 14 RAG V1 演示报告（后端文档问答）

- 生成时间（UTC）：2026-07-03T10:26:27.433856+00:00
- 语料文件：inputs/day8_corpus_backend_notes.txt
- chunk_size / overlap：80 / 20
- top-k：3
- candidate_top_n：8
- use_rerank：True
- embedding 模型：nomic-embed-text
- QA 模型：qwen2.5:0.5b

## Query: 如何排查数据库连接池打满导致的超时问题？

- citation_valid: True
- attempts: 1

### Top-K Chunks

- rank=1, chunk_id=2, score=0.7882
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- rank=2, chunk_id=4, score=0.4878
  - text: trace_id并串联调用链对于容器化部署CPU抢占和内存回收会影响尾延迟应检查节点负载容器限额GC日志以及是否存在突发扩缩容引起的冷启动接口设计上幂等和重试策略必须一起考虑只做重
- rank=3, chunk_id=6, score=0.4786
  - text: 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回chunk太小

### Answer

回答：
1. 请参考[chunk-2]，了解缓存参数和应用线程数的关联问题。
2. 请参考[chunk-4]，分析容器化部署对CPU抢占、内存回收的影响。
3. 请参考[chunk-6]，考虑高并发阶段限流和降级策略的优先级。

引用来源：
- [chunk-2] 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- [chunk-4] trace_id并串联调用链对于容器化部署CPU抢占和内存回收会影响尾延迟应检查节点负载容器限额GC日志以及是否存在突发扩缩容引起的冷启动接口设计上幂等和重试策略必须一起考虑只做重
- [chunk-6] 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回

## Query: 缓存命中率下降时应该优先看哪些指标？

- citation_valid: True
- attempts: 2

### Top-K Chunks

- rank=1, chunk_id=2, score=0.7812
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- rank=2, chunk_id=5, score=0.5512
  - text: 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见
- rank=3, chunk_id=0, score=0.4213
  - text: 在后端系统中日志排查通常从错误比例和延迟趋势开始首先看5xx比例是否短时间内上升其次看p95p99延迟是否抖动若两者同步升高通常说明依赖服务或资源池出现瓶颈数据库连接池是最

### Answer

回答：
缓存命中率下降时应该优先看以下指标：

- 数据库连接池的5xx比例是否短时间内上升
- 比如p95p99延迟是否抖动

引用来源：
- [chunk-2] 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- [chunk-5] 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见

## Query: 限流和降级策略在高并发场景下怎么落地？

- citation_valid: False
- attempts: 2

### Top-K Chunks

- rank=1, chunk_id=5, score=0.7882
  - text: 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见
- rank=2, chunk_id=6, score=0.7101
  - text: 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回chunk太小
- rank=3, chunk_id=2, score=0.5345
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时

### Answer

回答：
限流和降级策略在高并发场景下需要设计上幂等和重试策略，且幂等键生命周期应在存储层建立唯一约束，并且当系统进入高并发阶段时，限流和降级策略比横向扩容更快见效。此外，还应考虑缓存问题，如缓存命中率下降、数据库和远程服务请求会突增等问题。

引用来源：
- [chunk-5] 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间
- [chunk-6] 高并发阶段限流和降级策略比横向扩容更快见效
- [chunk-2] 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
