# Day 10 知识库问答 V1 报告（仅召回，不重排）

- 生成时间（UTC）：2026-07-03T02:13:53.893630+00:00
- 语料文件：inputs/day8_corpus_backend_notes.txt
- 查询文件：inputs/day9_queries.txt
- chunk_size / overlap：80 / 20
- top-k：3
- embedding 模型：nomic-embed-text
- QA 模型：qwen2.5:0.5b

## Query: 如何排查数据库连接池打满导致的超时问题？

### Retrieved Chunks

- rank=1, score=0.6251, chunk_id=2
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- rank=2, score=0.5964, chunk_id=4
  - text: trace_id并串联调用链对于容器化部署CPU抢占和内存回收会影响尾延迟应检查节点负载容器限额GC日志以及是否存在突发扩缩容引起的冷启动接口设计上幂等和重试策略必须一起考虑只做重
- rank=3, score=0.5963, chunk_id=6
  - text: 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回chunk太小

### Answer

当前信息不足，请提供更多的背景信息。

## Query: 缓存命中率下降时应该优先看哪些指标？

### Retrieved Chunks

- rank=1, score=0.6768, chunk_id=2
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- rank=2, score=0.6258, chunk_id=5
  - text: 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见
- rank=3, score=0.5912, chunk_id=0
  - text: 在后端系统中日志排查通常从错误比例和延迟趋势开始首先看5xx比例是否短时间内上升其次看p95p99延迟是否抖动若两者同步升高通常说明依赖服务或资源池出现瓶颈数据库连接池是最

### Answer

缓存命中率下降时，应优先检查以下指标：1. 数据库和远程服务请求的频率；2. 缓存key的设计过期策略、批量失效以及是否出现缓存穿透；3. 在微服务架构下，超时问题；4. 重复写入的问题。

## Query: 在微服务里如何定位超时链路的根因？

### Retrieved Chunks

- rank=1, score=0.6192, chunk_id=2
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时
- rank=2, score=0.6154, chunk_id=5
  - text: 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见
- rank=3, score=0.6107, chunk_id=6
  - text: 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回chunk太小

### Answer

在微服务架构中，超时链路的根因可能包括缓存命中率下降、数据库和远程服务请求突增等。建议检查缓存key设计过期策略、批量失效以及是否出现缓存穿透等问题，并且在微服务架构下应考虑幂等性和重试策略。

此外，在高并发阶段，限流和降级策略比横向扩容更快见效，优先保护核心接口而非非核心请求，可排队或返回兜底结果以避免雪崩并缩短故障恢复时间。

## Query: 限流和降级策略在高并发场景下怎么落地？

### Retrieved Chunks

- rank=1, score=0.7124, chunk_id=5
  - text: 口设计上幂等和重试策略必须一起考虑只做重试不做幂等可能导致重复写入建议明确幂等键生命周期并在存储层建立唯一约束当系统进入高并发阶段限流和降级策略比横向扩容更快见
- rank=2, score=0.6956, chunk_id=6
  - text: 高并发阶段限流和降级策略比横向扩容更快见效优先保护核心接口非核心请求可排队或返回兜底结果这样可以避免雪崩并缩短故障恢复时间RAG场景下文本切分质量直接影响召回chunk太小
- rank=3, score=0.6519, chunk_id=2
  - text: 待连接池参数和应用线程数缓存问题也会放大下游压力若缓存命中率下降数据库和远程服务请求会突增应检查缓存key设计过期策略批量失效以及是否出现缓存穿透在微服务架构下超时

### Answer

限流和降级策略在高并发场景下可以采取以下措施：

1. 建立唯一约束：当系统进入高并发阶段，限流和降级策略比横向扩容更快见效。
2. 保护核心接口：避免重复写入建议明确幂等键生命周期并在存储层建立唯一约束。
3. 避免雪崩并缩短故障恢复时间：在RAG场景下，文本切分质量直接影响召回。

以上措施需要结合实际需求和环境进行调整。
